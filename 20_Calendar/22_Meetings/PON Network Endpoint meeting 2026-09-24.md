Had a meeting with Eugene and Martin
Demoed calculator with endpoint on GIS. Described how we model PON networks.
Discussed how to use landmarks with PON. taps and splitters would be good landmarks.  Somehow landmarks need to be across the multiple paths. So instead of distances to distance table on a circuit. We need it to apply to a path.  Maybe landmarks on a cable.

This reminded me of the landmark creation screen and the mechanism to show SOR file on top and map on bottom.  I think this is what I was looking for on the OTDR mapping mechanism. If a SOR file is attached we can do this.

We also really need to come up with a way to build out this PON network. Come up with a file format that can import and export a fiber network.  Use the cable names and do it that way.
